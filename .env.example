# Stage 10 — base URL of the real API (the /server service).
# Unset = every adapter uses the local mock stores and the login screen
# runs in Stage 9 local-session mode (no password check).
# Local:  http://localhost:8090   ·  Deployed: the Render service URL
VITE_API_BASE_URL=

# Demo sign-in (Stage 10 Phase 2, NON-PRODUCTION): all 20 preset accounts
# share the password Aurora2026! — usernames like sara.rahman / maya.chen
# (see the login screen's demo directory or server/Data/users-seed.json).

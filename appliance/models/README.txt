The model ships HERE — alongside the package, never inside an image.
run.sh / run.ps1 download the OFFICIAL Qwen2.5-7B-Instruct-GGUF Q4_K_M
shards into this folder and sha256-verify them against the pinned
upstream digests; for a fully OFFLINE install, copy the two .gguf files
here from install media before running the script.

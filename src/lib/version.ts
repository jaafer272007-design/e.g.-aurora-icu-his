/* The app version — ONE source (previously a hardcoded string in the
   NavSidebar footer; Settings shows the same constant, never a second
   copy that could drift). */
export const APP_VERSION = 'AURORA HIS v4.2'
